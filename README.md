c program for local network chat system. where user can chat local with same network online/offline using hostop. 

#user can chat with same network user using cmd pannel, but need to run server too in one device and rest of devices can use that. buffer time 0 milesecond